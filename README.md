# flask-town

Standalone TOWN Render service.

This repository is separated from the main Flask/ORDER repository. It may share the same TiDB instance, while TOWN runtime code and tables remain logically isolated.
